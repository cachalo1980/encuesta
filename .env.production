# Entorno: producción (VPS + Cloudflare Tunnel)
# Este archivo SÍ está versionado. Viaja con el código en git.
# Para desplegar en el VPS: cp .env.production .env && docker compose up -d --build

ADMIN_PASSWORD=devmentor2024
ALLOWED_ORIGINS=https://encuesta.caruao.cloud
VITE_API_URL=http://encuesta.caruao.cloud:8001
