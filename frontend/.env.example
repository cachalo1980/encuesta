# Copia este archivo a .env y ajusta los valores según el entorno.
# En desarrollo local apunta al backend en localhost.
# En producción apunta a la URL pública del backend.
VITE_API_URL=http://localhost:8001
